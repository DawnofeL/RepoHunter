**一、需求理解与检索预筛**（`pre_filter/`，约 300 行） 需求转查询词、语言归一、并发检索去重、判定标准编译、gate 粗筛。这是流程入口，篇幅不大，写一条 bullet 够。

**二、Agent 探查引擎**（`repo_detection/` 里 explorer.py + tool_loop.py + agent_tools.py + audit.py，约 1150 行，全项目最重的一块） 四个只读工具、ReAct 主循环、预算控制与程序化提醒、并发信号量调度、锚点核验自愈闭环。这是你简历里最该占篇幅的技术核心，因为工程量和设计密度都在这。建议拆成两条 bullet：「Agent 工具编排与探查循环」+「Harness Engineering（锚点核验闭环）」分开写，不要合并，合并会把两个不同维度的设计（怎么探查 / 怎么保证探查结果可信）压成一条读不出层次。

**三、判定辩论引擎**（`repo_detection/` 里 debate.py + evidence.py + scoring.py，约 380 行） 分诊、按需取证、正反裁决、位置对齐。这是第二个技术亮点，独立一条 bullet。

**四、双层记忆系统**（`history/` + `memory/` 全部，约 1400 行，体量僅次于探查引擎） 这块你可能低估了它在简历里该占的分量，历史记忆（仓库账本复用）和对话记忆（compact 压缩、extract 提取、recall 召回）加起来行数比判定辩论还多，尤其 `compact/` 这套（token 阈值触发的会话笔记压缩、连败熔断硬截断）是个独立的技术点，值得单独一句而不是塞进"双层记忆系统"这一条里带过。

**成本与并发工程**贯穿二、三两块（前缀缓存、信号量、token 计量），不是独立板块，但值得单独一条 bullet，因为它是横切的性能设计,如果揉进"探查引擎"那条会显得那条太长。

所以整体建议是 **五大块、五条 bullet**：检索预筛（简写）、Agent 探查循环、Harness 锚点核验、判定辩论机制、Token/缓存工程、双层记忆系统——这样六条，把







- **AI 理论基础**:深入掌握 Transformer 底层架构原理(从理论到 LLaMA-3 源码完整拆解),对 SFT等后训练算法有深入理解并具备 LoRA 多轮对话范式训练实践,深刻了解深度学习(CNN、LSTM、Transformer)与传统机器学习(XGBoost、Random Forest、SVM 等)。



个人原创开源项目,面向需求的 GitHub 开源项目检索与分析 Agent,帮助开发者从庞大且质量参差的开源生态中精准定位契合需求的项目,并快速掌握其繁杂代码背后的核心设计。系统接收自然语言需求,自主完成候选检索、源码深度探查及报告生成、逐条需求判定与排序。针对 Claude Code 仅支持文件线性通读、缺乏标准化深度探查、无并行能力的局限,以标准化深度探查流程、对抗式核验与多仓库并行架构予以解决。编排层手写 asyncio 以贴合前缀缓存优化,FastAPI + SSE 服务端支持流式交互与全链路可观测。

- **检索与预筛架构**：将用户模糊需求拆解为搜索查询词并基于 GitHub MCP 进行多路并发检索与去重；对抓取的 README 做结构化降噪（剔除日志、图片与 url 等）后结合其余基础信息提前排除明显不符用户需求的项目。
- **Agent 工具设计与 ReAct 循环**：编写 grep 等代码探查工具供模型自主决策 tool calling，深度挖掘 Github 仓库并产出其宏观架构与核心设计总结。对 ReAct 循环进行多重兜底设计，如 LLM 低收益调用工具与死循环的熔断机制，对输出 JSON 解析失败等各类输出异常设计独立修复额度与降级抢救等；实现长链路工具探查的上下文滚动清理机制，以占位符归档历史工具返回、按需可逆重读。

- **多 Agent 编排对抗式分析与裁决**：并发将用户模糊需求重写为具备可判定边界的严谨表述以消除歧义；编排层将重写后的需求并行派发至持正反立场的子 Agent，双方独立检索并举证，证据锚定源码并经代码核验杜绝幻觉；独立裁决 Agent 依需求性质动态调整证据门槛，综合双方证据判定项目是否满足对应需求。

- **双层记忆系统**：设计对话中提取、跨 session 复用的三类通用记忆（用户偏好、反馈、项目），参考 Claude Code 源码实现索引常驻加正文按需加载的分层召回，由小模型语义匹配识别用户提及的项目，命中注入正文、歧义时输出候选待确认；Compact 机制在对话过长时分级降级，优先复用预写摘要、否则实时生成、再失败则截断保留最近对话；仓库架构拆解持久化至 SQLite，召回命中即复用。

- **可解释性与成本控制：** Agent 行为全程白盒可回溯，ReAct 各轮工具调用与对抗裁决日志持久化支持前端逐步回放，每轮注入模型的完整提示词分段可视，召回、提取、压缩决策均落审计库；围绕 LLM 前缀缓存设计提示词架构，并发扇出前串行预热保证后续缓存命中，实测完整检索流程缓存命中率约 60%，单次全流程 token 成本降低约 70%。





GitHub 开源项目检索与分析 Agent，帮助开发者从庞大且质量参差的开源生态中精准定位契合需求的项目，并快速掌握其繁杂代码背后的核心设计。 系统接收自然语言需求，自主完成候选检索、源码深度探查与逐条需求判定排序。针对 Claude Code 仅支持文件线性通读、缺乏标准化深度探查、无并行的局限，以标准化探查流程、对抗式核验与多仓库并行架构破解。编排层手写 asyncio 贴合前缀缓存优化，配备交互式 Web 界面与实时流式工作台，分析全程可监控。
