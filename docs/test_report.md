# 对话侧记忆功能真机测试报告（第一次执行）

日期 2026-07-08，用现有真库真 key 跑，搜索侧（A）没跑、用库里已有数据。结果 **18/18 全部通过**。数据已真实落进三个库，明天启动 webapp 就能在网页端看到。

本报告每条都标了「明天去哪看、该看到什么」，你启动 webapp 后对着点。

## 数据落在哪

- `data/memory.db` 的 chat_sessions：5 个测试会话
- `data/memory.db` 的 chat_memories：C 提取出的 1 条新记忆
- `data/dev_audit.db` 的 dev_records：8 个测试会话的召回和提取监控记录（重启后仍在）
- `data/history.db`：本次没动（搜索侧没跑）

## session_id 映射

| session_id | 测试 | 在会话列表 | 在 🔬 审计 |
|---|---|---|---|
| 20260708-140001 | B 手动注入 + 边界 | 有 | recall 4 / extract 4 |
| 20260708-140002 | C 记忆提取 | 有 | recall 2 / extract 2 |
| 20260708-140003 | C5 跨会话验证 | 无（只查索引没存会话） | recall 1 |
| 20260708-140004 | D1 语义命中召回 | 有 | recall 1 / extract 1 |
| 20260708-140005 | D2 抗误召回 | 无 | recall 1 |
| 20260708-140006 | D3 langgraph 召回 | 有 | recall 1 / extract 1 |
| 20260708-140007 | D4 记忆召回 | 有 | recall 1 / extract 1 |
| 20260708-140008 | D6 跳过门 | 无 | recall 1 |

会话列表看得到的是走了完整对话的（B/C/D1/D3/D4）。D2/D6 只查召回判断没存会话，所以只在 🔬 审计里有召回记录。

## 逐条结果

### 模块 B 手动注入（会话 140001）

- **B1 注入仓库拆解进 system**：注入 TradingAgents 问「反思机制怎么实现的」，system 里出现 TradingAgents 的拆解段。助手基于拆解答了「基于真实交易结果的复盘改进」。召回还顺带补了 GenAI_Agents（它也讲反思和记忆，属召回正常行为）。
- **B2 边界拒绝**：接着问「langgraph-supervisor 怎么样」（库里没有），助手回「这个我没分析过，去搜索页跑一次。」没有编造、没有触发搜索。
- **B3 会话落库**：chat_sessions 取回 4 条消息（两轮），会话完整存下。
- 明天看：会话列表点开 140001，看两轮对话；点某轮 assistant 头像（🍃），看注入的 TradingAgents 拆解段。

### 模块 C 记忆提取（会话 140002）

- **C1 说「记住」触发提取并落库**：说「记住：我做数据标注出身，看 agent 项目先看有没有清晰的评测指标」，后台提取出一条 user 记忆 `data-annotation-bg-eval-focus` 落库。
- **C2 密钥打码**：`scan_and_redact` 对含假 key 的文本扫描，命中 1 处，key 被换成 `[REDACTED]`。这是落库前的兜底防线。
- **C3 提取游标**：提取后游标挪到已处理位置（2），下次从新消息续，不重复。
- **C5 跨会话**：新会话不注入任何东西，C 提取的记忆以索引形式出现在 system。这条本次因重跑幂等（记忆已存）验证偏弱，但 D4 强证明了跨会话（见下）。
- 明天看：左栏「记忆」tab 或 Vault 记忆页，看到新记忆 `data-annotation-bg-eval-focus`；🔬 审计视图选 140002，看 extract 记录（提示词、模型原始返回、落库计数）。

### 模块 D 记忆召回（会话 140004-140008）

- **D1 语义命中**：不注入任何仓库，问「有没有做股票交易决策的多智能体项目」，召回靠语义挑中 TradingAgents（描述讲金融交易、股票买卖决策），并转正成右栏芯片。
- **D2 抗误召回**：问「有没有做视频剪辑的 rust 命令行工具」，库里没有，召回三档全空，没有硬塞一个多智能体框架充数。这是最关键的一条。
- **D3 langgraph 查询**：问「langgraph 相关的那个 swarm 库」，精准命中 langchain-ai/langgraph-swarm-py，没进歧义档，说明小模型够自信、能区分。
- **D4 记忆召回**：问「推荐个适合我的项目，你还记得我的偏好吧」，召回挑中两条记忆并补正文：`data-annotation-bg-eval-focus`（C 刚提取的）和 `prefers-lightweight-local-projects`（原有的）。这条同时证明了跨会话记忆生效，C 提取的记忆在另一个会话被召回用上。
- **D5 索引化**：确认 system 里的通用记忆段是「长期记忆索引」（每条一句摘要），不是全量正文。
- **D6 跳过门**：发「嗯」，召回不调模型，recall_debug 记 `fired: False, reason: 消息太短或纯应答`。
- 明天看：会话列表点开 140004（股票交易）、140006（langgraph）、140007（记忆召回），点某轮 assistant 头像，看 user 消息之后插入的「召回判断」块（命中仓库、命中记忆、分不清的候选、模型原始返回可折叠）；140004 右栏还能看到召回转正的 TradingAgents 芯片（带「召回」小标）。

### 模块 E 监控

- **E1 提示词分段**：prompt 事件带 segments（分段的 system）+ messages，前端点头像靠它渲染。
- **E2 提取和召回监控**：dev 审计里 extract 和 recall 两类记录都在，落库持久。
- 明天看：🔬 开发者抽屉打开，切「审计」视图，按 session 看每轮召回怎么判的、提取抠了什么。

## 模块 F webapp API 链路（第二次执行，已完成）

启动后端 curl 各端点，前端拿数据的链路全部验证通：

- `GET /health` → `{"status":"ok"}`
- `GET /chat/session` → 5 个测试会话，title 是每次对话的首句
- `GET /chat/session/20260708-140004` → 完整还原 D1 会话，助手详细讲了 TradingAgents 的五阶段决策流程（分析、多空辩论、交易意向、风险辩论、最终决策）
- `GET /memory` → 12 个仓库（11 有拆解 + 1 gate 跳过），前端仓库页数据源
- `GET /history` → 2 条搜索记录
- `GET /chat/dev/audit` → 8 个测试 session 的审计索引
- `GET /chat/dev/audit/20260708-140004` → D1 的召回审计记录：fired True，picks TradingAgents，清单 14 条（11 仓库 + 3 记忆），这正是明天 🔬 审计视图渲染的数据

进程验证完已干净关闭，端口 8755 已释放，没留 stale process。

明天你启动 webapp（`python run.py`，会自动唤起浏览器）就能看到上面这些数据，对着「session_id 映射」表逐个点。

## 没跑的

搜索侧（模块 A）没跑，用的是库里已有的 11 个拆解仓库。要验证搜索去重复用（跑一次真实搜索看 repo_memory 追加 seen_runs、runs 新增一行），是第三次执行，你点头才跑，因为最贵。

## 硬限制说明

浏览器渲染的截图给不了（没有浏览器和截图工具）。以上都是真实数据落库和函数返回的证据，网页端渲染成什么样明天你自己验收，数据都已经在库里等着。
